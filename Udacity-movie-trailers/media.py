#import webbrowser to allow the opneing of web browsers, 
import webbrowser

#create a class movie that will have the movie contents to be initialized for different movies in enternainment.py
class Movie():
	def _init_(self, movie_title, movie_storyline, poster_image_url, trailer_youtube_url)
		self.title = movie.title
		self.storyline = movie.storyline
		self.poster_image_url = movie.poster_image_url
		self.trailer_youtube_url = movie.trailer_youtube_url

def show_trailer(self):
	webbrowser.open(self.trailer_youtube_url)